LuneOS Local Manifest
===============

Local manifest used for LuneOS to remove hybris-boot and halium-boot from Halium build for LuneOS, since these are not being used by LuneOS.

### Usage

* `./setup
